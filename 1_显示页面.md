# 显示页面

设备树中修改

&dsi0 {
	status = "okay";
	//rockchip,lane-rate = <1000>;
	dsi0_panel: panel@0 {
		status = "okay";
		compatible = "simple-panel-dsi";
		reg = <0>;
		backlight = <&backlight>;  //背光pwm
		reset-delay-ms = <60>;//复位时间
		enable-delay-ms = <60>;//供电使能后时间
		prepare-delay-ms = <60>;
		unprepare-delay-ms = <60>;
		disable-delay-ms = <60>;
		init-delay-ms = <60>;//复位后等待多少时间发参数
		reset-gpios = <&gpio0 RK_PC6 GPIO_ACTIVE_LOW>;//复位引脚控制
		width-mm = <68>;
		height-mm = <121>;
		dsi,flags = <(MIPI_DSI_MODE_VIDEO | MIPI_DSI_MODE_VIDEO_BURST |
			MIPI_DSI_MODE_LPM | MIPI_DSI_MODE_EOT_PACKET)>;
		dsi,format = <MIPI_DSI_FMT_RGB888>;
		dsi,lanes  = <4>;
		panel-init-sequence = [
			23 00 02 B0 01

​			//23是控制命令，00delay，02数据长度，B0寄存器，01是寄存器参数，看数据手册

​			23 00 02 C0 26
​			23 00 02 C1 10
​			......
​			23 00 02 C0 11
​			05 78 01 11
​			05 1E 01 29

​			//掉电参数,一般不用填或修改

​		];

		panel-exit-sequence = [
			05 00 01 28
			05 00 01 10
		];
	
		disp_timings0: display-timings {
			native-mode = <&dsi0_timing0>;
			dsi0_timing0: timing0 {
				clock-frequency = <160000000>;
	
				hactive = <1200>;
				vactive = <1920>;
				hfront-porch = <60>;
				hsync-len = <16>;
				hback-porch = <80>;
				vfront-porch = <12>;
				vsync-len = <8>;
				vback-porch = <16>;
				hsync-active = <0>;
				vsync-active = <0>;
				de-active = <0>;
				pixelclk-active = <1>;
			};
		};
	
		ports {
			#address-cells = <1>;
			#size-cells = <0>;
	
			port@0 {
				reg = <0>;
				panel_in_dsi: endpoint {
					remote-endpoint = <&dsi_out_panel>;
				};
			};
		};
	};
	
	ports {
		#address-cells = <1>;
		#size-cells = <0>;
	
		port@1 {
			reg = <1>;
			dsi_out_panel: endpoint {
				remote-endpoint = <&panel_in_dsi>;
			};
		};
	};

};

![e04d93e54d7795fd13dd82bc26869aa2](C:\Users\Administrator\Documents\xwechat_files\wxid_0z24rjugjfwp22_9d24\temp\RWTemp\2025-08\1952b576c2f3bec6bf073ebffc508333\e04d93e54d7795fd13dd82bc26869aa2.png)

			//clock-frequency = htol x vtol x 帧率
			//htol=hactive+hbp+hfp+hsync
			//vtol=vactive+vbp+vfp+vbp